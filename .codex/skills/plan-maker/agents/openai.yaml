interface:
  display_name: "Plan Maker"
  short_description: "Create readable overview-first implementation plans."
  default_prompt: "Use $plan-maker to produce Korean-first self-contained executable plan files under ./plans from already locked request scope, execution areas, public contracts, verification responsibilities, and exclusions."
