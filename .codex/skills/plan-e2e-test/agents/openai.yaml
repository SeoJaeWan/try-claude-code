interface:
  display_name: "Plan E2E Test"
  short_description: "Generate frozen feature-level browser-integration Playwright plans after browser contracts resolve."
  default_prompt: "Use $plan-e2e-test to generate frozen feature-level browser integration Playwright test files under ./plans/{task}/e2e/ after all browser contracts are resolved."

policy:
  allow_implicit_invocation: true
