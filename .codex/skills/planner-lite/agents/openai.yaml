interface:
  display_name: "Planner Lite"
  short_description: "Execute plan.md phases by orchestrating child agents in isolated worktrees."
  default_prompt: "Use $planner-lite to execute a finalized plan.md by orchestrating frontend-developer, backend-developer, or publisher child agents inside per-track worktrees, with phase commits and final merge coordination."

policy:
  allow_implicit_invocation: true
