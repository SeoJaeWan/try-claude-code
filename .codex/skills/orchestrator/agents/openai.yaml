interface:
  display_name: "Orchestrator"
  short_description: "Run planning through architect, review, browser approval, and materialize."
  default_prompt: "Use $orchestrator to orchestrate the stateless artifact-driven architect -> review -> browser developer review -> revise -> materialize planning workflow, recording whether materialization passed or failed its targeted gate."
