interface:
  display_name: "Figma Inventory Snapshot"
  short_description: "Capture resumable Figma tree inventory snapshots"
  default_prompt: "Use $figma-inventory-snapshot to capture controller-verified Figma node inventory snapshots as checkpointed, manifest-backed local evidence artifacts."
