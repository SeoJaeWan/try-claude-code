interface:
  display_name: "Figma Inventory Snapshot"
  short_description: "Capture discovery-first shard-backed Figma JSON inventory snapshots"
  default_prompt: "Use $figma-inventory-snapshot to discover an arbitrary Figma file's actual structure, generate a shard extraction plan, write implementation-ready JSON inventory artifacts, and verify coverage in the manifest."
