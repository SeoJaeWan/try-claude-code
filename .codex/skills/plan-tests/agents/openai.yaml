interface:
  display_name: "Plan Tests"
  short_description: "Generate constraint-mapped unit test files as plan artifacts."
  default_prompt: "Use $plan-tests to generate unit test files mapped to plan.md constraint IDs, stored under ./plans/{task}/tests/."

policy:
  allow_implicit_invocation: true
