프로필 조회 캐시에 동일 사용자에 대한 동시 요청 중복 제거를 추가해줘.

완료 조건:

- 같은 `userId`로 동시에 호출하면 실제 API는 한 번만 호출된다.
- 서로 다른 `userId`의 요청은 독립적으로 실행된다.
- 실패한 요청은 캐시하지 않고 다음 호출에서 다시 시도한다.
- 기존 TTL 캐시 동작은 유지한다.
- 새로운 의존성은 추가하지 않는다.
- 관련 테스트와 기존 테스트가 모두 통과해야 한다.

구현 방법은 기존 프로젝트 구조에 맞게 자유롭게 결정해줘.
