interface:
  display_name: "Evaluate Workbench"
  short_description: "Benchmark Workbench targets on two fixed tasks"
  default_prompt: "Use $evaluate-workbench to compare selected Workbench targets on the fixed logic and frontend benchmarks."
policy:
  allow_implicit_invocation: false
