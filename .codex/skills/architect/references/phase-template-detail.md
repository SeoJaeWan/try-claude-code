# Phase {n}. {역할 이름}

> 이 문서는 실행용 상세 계약이다. 맨 위의 요약만 읽어도 컨트롤러가 이 phase의 목표, 실제 작업, 완료 판단, 중단 시점을 알 수 있어야 한다.
> 그 아래 섹션은 `plan.md`의 같은 phase 요약을 기술적으로 확장하되, 범위나 결론을 새로 바꾸지 않는다.

- 한 줄 목표:
- 실제 작업:
    - `{이 phase에서 실제로 바꾸는 경계 1}`
    - `{이 phase에서 실제로 바꾸는 경계 2}` (선택)
- 완료 증거:
    - `{컨트롤러가 확인할 결과 1}`
    - `{컨트롤러가 확인할 결과 2}` (선택)
- 중단 조건:
    - `{재계획 또는 즉시 중단이 필요한 조건}` (없으면 `없음`)

- owner_agent: `{agent-name}`
- 목적:
- boundary:
- input:
- output:
    - 공개 계약:
    - 내부 기본값: (선택)
    - 허용하지 않는 대안: (선택)
- 선행조건: `none` (선택)
- 제약: (선택)
- side effects: (선택)
- failure/validation: `{blocked case, rejected or losing path, side-effect guard, or ambiguity note when relevant}` (선택)
- 작업:
- 검증:
    - [ ] `{command 또는 확인 방법}`
    - [ ] `{기대 결과}`
