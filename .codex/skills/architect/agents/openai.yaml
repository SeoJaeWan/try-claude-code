interface:
  display_name: "Architect"
  short_description: "Create controller-friendly plans after resolving blocking ambiguity."
  default_prompt: "Use $architect to resolve blocking ambiguity and produce controller-friendly plan summaries with an overall work map plus per-phase technical detail files under ./plans."
