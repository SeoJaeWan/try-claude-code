interface:
  display_name: "Architect"
  short_description: "Create readable shape-first implementation plans."
  default_prompt: "Use $architect to resolve blocking ambiguity and produce readable sequential plans with request scope, change shape, locked contracts, phase flow, risks, and linked detail files under ./plans."
