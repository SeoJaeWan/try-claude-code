interface:
  display_name: "Dev Wiki"
  short_description: "Refresh the bundle or maintain a project wiki."
  default_prompt: "Use $workbench:dev-wiki to refresh the central Dev Wiki source bundle or maintain an explicitly scoped project wiki."

policy:
  allow_implicit_invocation: false
