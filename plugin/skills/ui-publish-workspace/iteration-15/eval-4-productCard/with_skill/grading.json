{
  "expectations": [
    {
      "text": "ProductCard 구현이 하나의 source of truth로 정리된다",
      "passed": true,
      "evidence": "Only one output file exists: outputs/index.tsx containing a single ProductCard component definition with a single ProductCardProps interface and single default export."
    },
    {
      "text": "flat components/ProductCard.tsx 파일이 남지 않는다",
      "passed": true,
      "evidence": "No flat ProductCard.tsx file exists in the outputs directory. The only file is index.tsx, consistent with a folder-based pattern."
    },
    {
      "text": "최종 구조가 ProductCard/index.tsx 패턴을 따른다",
      "passed": true,
      "evidence": "The output file is named index.tsx, following the folder/index.tsx convention. The file is placed at outputs/index.tsx which would correspond to ProductCard/index.tsx in the project structure."
    },
    {
      "text": "Props 인터페이스가 ProductCardProps를 유지한다",
      "passed": true,
      "evidence": "Line 5: 'export interface ProductCardProps {' — the interface is named ProductCardProps and is exported."
    },
    {
      "text": "사용처 import path가 일관된다",
      "passed": true,
      "evidence": "Single file output with default export (line 59: 'export default ProductCard'). With the folder/index.tsx pattern, all imports would resolve to the same path. No conflicting import sources exist."
    },
    {
      "text": "hover, image, price 등 visual affordance가 보존된다",
      "passed": true,
      "evidence": "Hover: 'hover:shadow-md' on container (line 26) and 'group-hover:scale-105' on image (line 35). Image: next/image Image component with fill, object-cover, aspect-square container (lines 31-37). Price: price displayed with currency symbol and toLocaleString('ko-KR') formatting (lines 51-52)."
    },
    {
      "text": "useEffect, fetch, axios, useQuery, useMutation 호출이 없다",
      "passed": true,
      "evidence": "Grep for useEffect|fetch(|axios|useQuery|useMutation returned 0 matches across all output files."
    },
    {
      "text": "컴포넌트가 const 화살표 함수로 선언된다",
      "passed": true,
      "evidence": "Line 15: 'const ProductCard = ({' with arrow function syntax on line 23: '}: ProductCardProps) => {'"
    },
    {
      "text": "컴포넌트 폴더명이 camelCase이다 (productCard)",
      "passed": false,
      "evidence": "The output file is placed directly in outputs/index.tsx with no subfolder. There is no evidence that the folder is named 'productCard' (camelCase). The eval name references 'productCard' but the actual output structure does not include a camelCase folder — it is just a bare index.tsx in the outputs directory."
    },
    {
      "text": "tcp validate-file이 실행되어 0 violations을 확인했다",
      "passed": false,
      "evidence": "The transcript file (ab4127f8e8bac7a15.output) is empty (0 bytes). No evidence that tcp validate-file was executed or that 0 violations were confirmed."
    }
  ],
  "summary": {
    "passed": 8,
    "failed": 2,
    "total": 10,
    "pass_rate": 0.80
  }
}
