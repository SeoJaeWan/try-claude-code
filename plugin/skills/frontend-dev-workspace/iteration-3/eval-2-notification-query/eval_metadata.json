{
  "eval_id": 2,
  "eval_name": "notification-query",
  "prompt": "알림 조회 로직을 프로젝트 컨벤션에 맞게 정리해줘. NotificationList는 query 결과를 소비하는 쪽에 집중하고, 조회 로직은 훅 경계로 정리해줘.",
  "assertions": [
    {"id": "query-hook-path", "text": "조회 훅이 hooks/apis/{domain}/queries/ 경로 아래에 배치된다 (tcf profile: API query hook -> hooks/apis/{domain}/queries)"},
    {"id": "hook-index-ts", "text": "훅이 hooks/apis/{domain}/queries/{hookName}/index.ts 패턴으로 생성된다 (tcf profile: output = {path}/{name}/index.ts)"},
    {"id": "no-fetch-in-component", "text": "NotificationList 컴포넌트에 직접적인 useEffect+fetch 패턴이 남지 않는다"},
    {"id": "loading-state", "text": "loading 상태 분기가 유지된다"},
    {"id": "error-state", "text": "error 상태 분기가 유지된다"},
    {"id": "empty-state", "text": "empty state 분기가 유지된다"},
    {"id": "hook-use-prefix", "text": "query 훅 이름이 useGet 접두사를 사용한다 (tcf profile: API query naming = useGet*)"},
    {"id": "arrow-function", "text": "훅이 화살표 함수로 정의된다 (shared profile: function style = arrow)"}
  ]
}