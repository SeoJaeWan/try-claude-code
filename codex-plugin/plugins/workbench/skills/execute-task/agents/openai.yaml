interface:
  display_name: "Execute Task"
  short_description: "Coordinate Luna/xhigh workers with Sol/high"
  default_prompt: "Use $workbench:execute-task with Luna/xhigh workers to exhaust this task plan's safely runnable work and report implementation-time findings; Sol/high is the recommended coordinator."

policy:
  allow_implicit_invocation: false
