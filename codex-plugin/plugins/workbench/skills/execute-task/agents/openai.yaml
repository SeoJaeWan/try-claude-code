interface:
  display_name: "Execute Task"
  short_description: "Execute task DAGs with selected worker profiles"
  default_prompt: "Use $workbench:execute-task to run this plan with its task profiles, continue independent work, and handle findings and requirement updates."

policy:
  allow_implicit_invocation: false
