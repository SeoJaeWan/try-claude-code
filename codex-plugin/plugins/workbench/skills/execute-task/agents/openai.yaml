interface:
  display_name: "Execute Task"
  short_description: "Run a task plan with Sol/high workers"
  default_prompt: "Use $workbench:execute-task to run this task plan through parallel GPT-5.6 Sol/high workers."

policy:
  allow_implicit_invocation: false
