interface:
  display_name: "Prepare"
  short_description: "Plan tasks, dependencies, checks, and worker profiles"
  default_prompt: "Use $workbench:prepare to plan this change with task-specific models, efforts, isolated worktrees, and verification."

policy:
  allow_implicit_invocation: false
