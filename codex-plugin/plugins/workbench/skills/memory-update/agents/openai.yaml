interface:
  display_name: "Memory Update"
  short_description: "Curate local knowledge with research and safe edits"
  default_prompt: "Use $workbench:memory-update to investigate these knowledge topics and update local .codocs with one writer and verified references."

policy:
  allow_implicit_invocation: false
