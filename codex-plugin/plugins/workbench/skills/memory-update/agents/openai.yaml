interface:
  display_name: "Memory Update"
  short_description: "Curate local .codocs knowledge and references"
  default_prompt: "Use $workbench:memory-update to update these bounded project-knowledge topics only in local .codocs documents, following their authoring rules and verifying references."

policy:
  allow_implicit_invocation: false
