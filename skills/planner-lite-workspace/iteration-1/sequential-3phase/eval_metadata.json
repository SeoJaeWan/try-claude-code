{
  "eval_id": 1,
  "eval_name": "sequential-3phase",
  "prompt": "plan.md에 정의된 3개 Phase를 worktree 격리 방식으로 실행해줘",
  "assertions": [
    {"id": "reads-skill", "text": "SKILL.md를 먼저 읽었는가"},
    {"id": "validates-branch-header", "text": "Branch: 헤더를 검증했는가"},
    {"id": "validates-owner-agents", "text": "owner_agent 참조가 agents/*.md에 존재하는지 검증했는가"},
    {"id": "creates-worktree", "text": "git worktree add -b 로 태스크 브랜치를 생성했는가"},
    {"id": "sequential-execution", "text": "Phase를 순차적으로 실행했는가"},
    {"id": "phase-commits", "text": "각 Phase마다 별도 커밋을 생성했는가"},
    {"id": "merge-no-ff", "text": "git merge --no-ff 로 최종 병합했는가"},
    {"id": "cleanup-worktree", "text": "worktree 정리 후 브랜치 삭제했는가"}
  ]
}
