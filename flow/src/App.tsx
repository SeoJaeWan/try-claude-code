export function App() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100%",
        background: "var(--color-bg)",
        color: "var(--color-text-primary)",
      }}
    >
      <p>Codex Workflow 3D — loading...</p>
    </div>
  );
}
