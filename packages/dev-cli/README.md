# @seojaewan/dev-cli-core

Shared runtime for the published `@seojaewan/frontend` and `@seojaewan/backend` packages.

This package is intended to be consumed by the scoped CLI wrappers, not installed directly in most cases.
